services:

  php:
    build: .
    container_name: pin40prescription_php
    restart: unless-stopped
    ports:
      - "80:80"
    #  - "443:443"
    #  - "443:443/udp"
    volumes:
      - ./:/app
    environment:
      - SERVER_NAME=:80
    depends_on:
      - db
    networks:
      - prescriptpin40

  # Base de données MariaDB
  db:
    image: mariadb:10.11.2
    container_name: pin40prescription_db
    restart: unless-stopped
    environment:
      - MYSQL_DATABASE=prescrippin40_db
      - MYSQL_USER=xavier
      - MYSQL_PASSWORD=Corwin_40280
      - MYSQL_ROOT_PASSWORD=Corwin_40280
    ports:
      - "3307:3306"
    volumes:
      - db_data:/var/lib/mysql
    networks:
      - prescriptpin40

  # phpMyAdmin pour gérer la base de données
  phpmyadmin:
    image: phpmyadmin/phpmyadmin:latest
    container_name: pin40prescription_pma
    restart: unless-stopped
    ports:
      - "8080:80"
    environment:
      - PMA_HOST=db
      - PMA_PORT=3306
      - MYSQL_ROOT_PASSWORD=Corwin_40280
    depends_on:
      - db
    networks:
      - prescriptpin40

volumes:
  db_data:

networks:
  prescriptpin40:
    external: true

