{{ form_start(form) }}

<div class="row g-3">

    <div class="col-md-6">
        {% include 'composants/inputs/input_select.html.twig' with {
            label: 'Type d’équipement',
            field: form.typeEquipment
        } %}
    </div>

    <div class="col-md-6">
        {% include 'composants/inputs/input_text.html.twig' with {
            label: 'Marque',
            field: form.brandEquipment
        } %}
    </div>

    <div class="col-md-6">
        {% include 'composants/inputs/input_text.html.twig' with {
            label: 'Matricule',
            field: form.matriculEquipment
        } %}
    </div>

    <div class="col-md-6">
        {% include 'composants/inputs/input_select.html.twig' with {
            label: 'OS installé',
            field: form.osInstalled
        } %}
    </div>

    <div class="col-md-4">
        {% include 'composants/inputs/input_select.html.twig' with {
            label: 'Statut',
            field: form.statusEquipment
        } %}
    </div>

    <div class="col-4 pt-4 mt-4 ps-4">
        {% include 'composants/forms/check_verif.html.twig' with {
            label: 'Disponible',
            field: form.isDispo
        } %}
    </div>

    <div class="col-12">

        <a class="btn btn-secondary" href="{{ path('app_admin_dashboard_index') }}">Retour</a>
        <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>

    </div>

</div>

{{ form_end(form) }}
