{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="text-center mb-4">Bienvenue sur votre tableau de bord</h2>

        <div class="row">
            <div class="col-9">
                <div class="card">
                    <div class="card-body">
                        <p class="lead">Machine disponibles</p>
                        <p class="h3">{{ equipments|length }}</p>
                    </div>
                </div>
            </div>
            <div class="col-3">
                <a href="{{ path('app_admin_member_new',{'role':'prescripteur'}) }}" class="btn btn-outline-info">Ajouter un prescripteur</a>
                <a href="{{ path('app_admin_member_new',{'role':'mediateur'}) }}" class="btn btn-outline-info">Ajouter un médiateur</a>
            </div>
        </div>

        <div class="row">
            <div class="col-12">
                <p>LISTE DES PRESCRITIONS</p>
                <table class="table table-striped table-hover">
                    <thead>
                        <tr>
                            <th></th>
                            <th>ref</th>
                            <th>Nom et Prénom</th>
                            <th>Structure</th>
                            <th>Espace</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for prescription in prescriptions %}
                            <tr>
                                <td></td>
                                <td>{{ prescription.ref }}</td>
                                <td>{{ prescription.beneficiaire.firstname }} {{ prescription.beneficiaire.lastname }}</td>
                                <td>{{ prescription.membre.nameStructure }}</td>
                                <td>{{ prescription.lieuMediation }}</td>
                            </tr>
                        {% else %}
                            <tr>
                                <td colspan="5">Pour l'instant il n'y a pas de prescription</td>
                            </tr>
                        {% endfor %}
                    </tbody>
                    <tfoot></tfoot>
                </table>

            </div>
        </div>

        <div class="row justify-content-center text-center g-4">

            {# 👉 Si le user est ADMIN : afficher les 2 cartes #}
            {% if is_granted('ROLE_ADMIN') %}
                <div class="col-md-4">
                    <div class="card h-100 shadow-sm">
                        <img
                            src="{{ asset('assets/images/dashboard_beneficiary.jpg') }}"
                            class="card-img-top mx-auto d-block mt-3"
                            alt="Médiation"
                            style="width: 100px; height: 100px; object-fit: contain;"
                        >
                        <div class="card-body">
                            <h6 class="card-title">Médiation</h6>
                            <a href="{{ path('app_gestapp_equipment_index') }}" class="btn btn-primary mt-2">Voir plus</a>
                        </div>
                    </div>
                </div>

                <div class="col-md-4">
                    <div class="card h-100 shadow-sm">
                        <img
                            src="{{ asset('assets/images/dashboard_prescription.jpg') }}"
                            class="card-img-top mx-auto d-block mt-3"
                            alt="Prescription"
                            style="width: 100px; height: 100px; object-fit: contain;"
                        >
                        <div class="card-body">
                            <h6 class="card-title">Prescription</h6>
                            <a href="{{ path('app_gestapp_prescription_index') }}" class="btn btn-primary mt-2">Voir plus</a>
                        </div>
                    </div>
                </div>

                {#  afficher uniquement la carte Médiation #}
            {% elseif is_granted('ROLE_MEDIATEUR') %}
                <div class="col-md-4">
                    <div class="card h-100 shadow-sm">
                        <img
                            src="{{ asset('assets/images/dashboard_beneficiary.jpg') }}"
                            class="card-img-top mx-auto d-block mt-3"
                            alt="Médiation"
                            style="width: 100px; height: 100px; object-fit: contain;"
                        >
                        <div class="card-body">
                            <h6 class="card-title">Médiation</h6>
                            <a href="{{ path('app_gestapp_equipment_index') }}" class="btn btn-primary mt-2">Voir plus</a>
                        </div>
                    </div>
                </div>

                {#  afficher uniquement la carte Prescription #}
            {% elseif is_granted('ROLE_PRESCRIPTEUR') %}
                <div class="col-md-4">
                    <div class="card h-100 shadow-sm">
                        <img
                            src="{{ asset('assets/images/dashboard_prescription.jpg') }}"
                            class="card-img-top mx-auto d-block mt-3"
                            alt="Prescription"
                            style="width: 100px; height: 100px; object-fit: contain;"
                        >
                        <div class="card-body">
                            <h6 class="card-title">Prescription</h6>
                            <a href="{{ path('app_gestapp_prescription_index') }}" class="btn btn-primary mt-2">Voir plus</a>
                        </div>
                    </div>
                </div>
            {% endif %}
        </div>
    </div>
{% endblock %}
